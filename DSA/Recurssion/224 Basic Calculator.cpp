/*off the bat we can see that the whole problem can be solved if we solve the inner bracket and if we can solve the inner we solve the outside one also , form left to right as we go */

#include<bits/stdc++.h>
using namespace std;
int solve(String s){
            
}
int main(){
    string s;
    cin>>s;
    
    return solve(s);
}